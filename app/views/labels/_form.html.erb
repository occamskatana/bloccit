<div class="form-group">
	<%= f.label :labels %>
	<%= f.text_field :labels, 
										class: 'form-control', 
										placeholder: "Enter Labels (seaparted by commas)", 
										value: labels.map(&:name).join(', ') %>
	</div>
